module.exports = {
  // parser: file.extname === ".sss" ? "sugarss" : false,
  plugins: {
    'postcss-import': {},
    'postcss-preset-env': {},
    // cssnano: env === "production" ? options.cssnano : false
  },
};
